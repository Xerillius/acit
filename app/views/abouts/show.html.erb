<div class="container-fluid">
	<div class="row">
		<div class="col-xs-12 col-sm-6 col-sm-offset-3">
			<div class="panel">
				<div class="panel-heading">
					<%= @about.title %>
				</div>
				<div class="panel-body">
					<%= @about.content %>
				</div>
				<div class="panel-footer">
					<%= link_to 'Edit', edit_about_path(@about), :class => "btn", :id => "btn" %>
					<%= link_to 'Back', abouts_path, :class => "btn", :id => "btn" %>
				</div>
			</div>
		</div>
	</div>
</div>